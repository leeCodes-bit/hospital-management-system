<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://pro.fontawesome.com/releases/v5.10.0/css/all.css"
      integrity="sha384-AYmEC3Yw5cVb3ZcuHtOA93w35dYTsvhLPVnYs9eStHfGJvOvKxVfELGroGkvsg+p"
      crossorigin="anonymous"
    />

    <!-- Custom StyleSheet -->
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Signup page</title>
  </head>
  <body>
    <section class="gradient-custom-2">
      <div class="container h-100">
        <div class="row d-flex justify-content-center align-items-center">
          <div class="col-lg-12 col-xl-11 form-body">
            <div class="card text-black" style="border-radius: 10px">
              <div class="card-body p-md-4">
                <div class="row justify-content-center">
                  <div class="col-md-10 col-lg-6 col-xl-5 order-2 order-lg-1">
                    <p class="text-center h1 fw-bold mb-5 mx-1 mx-md-4 mt-4">
                      Sign up
                    </p>

                    <form class="mx-1 mx-md-4" method="POST" action="/doctor/signup">
                      <div class="d-flex flex-row align-items-center mb-4">
                        <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                        <div class="form-outline flex-fill mb-0">
                          <label class="form-label" for="doctorsName">Name</label>
                          <% if(locals.error){ %>
                            <% if(error === 'doctorsName is not allowed to be empty'){ %>
                            <div style="
                            color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            ">Name not allowed to be empty</div>
                            <% } %>
                             
                          <% } %>
                          <input type="text" id="doctorsName" class="form-control" name="doctorsName"/>
                         
                        </div>
                      </div>

                      <div class="d-flex flex-row align-items-center mb-4">
                        <i class="fas fa-envelope fa-lg me-3 fa-fw"></i>
                        <div class="form-outline flex-fill mb-0">
                          <label class="form-label" for="email">Your Email</label>

                          <% if(locals.error){ %>
                            <% if(error === "email is not allowed to be empty"){ %>
                            <div style="
                            color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            "><%= error %></div>
                            <% } else if (locals.error) {%>
                              <div style="
                            color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            ">Email already exist please change</div>
                           <% } %>
                          <% } %>
                          <input type="email" id="email" class="form-control" name="email"/>
                          
                        </div>
                      </div>

                      <div class="d-flex flex-row align-items-center mb-4">
                        <i class="fa fa-user-md fa-lg me-3 fa-fw"></i>
                        <div class="form-outline flex-fill mb-0">
                          <label class="form-label" for="specialization">Specialization</label>

                          <% if(locals.error){ %>
                            <% if(error === "specialization is not allowed to be empty"){ %>
                            <div style="color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            "><%= error %></div>
                            <%}%>
                              
                          <% } %>
                          <input type="text" id="specialization" class="form-control" name="specialization"/>
                          
                        </div>
                      </div>

                      <div class="d-flex flex-row align-items-center mb-4">
                        <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                        <div class="form-outline flex-fill mb-0">
                          <label class="form-label" for="gender">Gender</label>

                          <% if(locals.error){ %>
                            <% if(error === "gender is not allowed to be empty"){ %>
                            <div style="color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            "><%= error %> </div>
                            <%}%>
                              
                           
                          <% } %>
                          <input type="text" id="gender" class="form-control" name="gender"/>
                         
                        </div>
                      </div>

                      <div class="d-flex flex-row align-items-center mb-4">
                        <i class="fas fa-phone-square fa-lg me-3 fa-fw"></i>
                        <div class="form-outline flex-fill mb-0">
                          <label class="form-label" for="phoneNumber">Phone Number</label>

                          <% if(locals.error){ %>
                            <% if(error === "phoneNumber is not allowed to be empty"){ %>
                            <div style="color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            "><%= error %></div>
                            <%}%>

                            <% } %>
                          <input type="number" id="phoneNumber" class="form-control" name="phoneNumber"/>       
                         
                        </div>
                      </div>

                      <div class="d-flex flex-row align-items-center mb-4">
                        <i class="fas fa-lock fa-lg me-3 fa-fw"></i>
                        <div class="form-outline flex-fill mb-0">
                          <label class="form-label" for="password">Password</label>

                          <% if(locals.error){ %>
                            <% if(error === "password is not allowed to be empty"){ %>
                            <div style="color: #ff2200; 
                            margin: 10px 2px; 
                            font-size: 0.8em; 
                            font-weight: bold;
                            "> <%= error %></div>
                            <%}%>
                             
                            
                          <% } %>
                          <input type="password" id="password" class="form-control" name="password"/>
                       
                        </div>
                      </div>

                      <div
                        class="form-check d-flex justify-content-center mb-5"
                      >
                        <label class="form-check-label" for="login">
                          Do you have an account? <a href="/login">Login</a>
                        </label>
                      </div>

                      <div
                        class="d-flex justify-content-center mx-4 mb-3 mb-lg-4"
                      >
                        <button type="submit" class="btn btn-primary btn-lg">
                          Sign Up
                        </button>
                      </div>
                    </form>
                  </div>
                  <div
                    class="col-6 col-xl-7 d-flex align-items-center order-1 order-lg-2"
                  >
                    <img
                      src="https://static.vecteezy.com/system/resources/previews/003/659/519/original/doctor-checking-a-patient-in-hospital-room-background-illustration-medical-treatment-with-patients-for-healthcare-consultation-and-examination-health-vector.jpg"
                      class="img-fluid"
                      alt="Sample image"
                    />
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <script></script>
  </body>
</html>